# Reference lab stack validation — 2026-09-07

The release scope is a source-verified SDL1 virtual workflow and a separate
bounded FastCat empirical predictor. Physical calibration is not a release
prerequisite under the revised scope approved on 2026-09-07. The removed estimator and failed FastCat interval revision remain failed
historical results. Version 0.1.21 is
staged; merge and publication still require explicit approval.

The scientific objective remains learning an unfamiliar physical system through
interventions capable of contradicting predictions. Completing a workflow,
recording a prospective prediction or selecting the twin's argmin does not prove
that capability. The CLI owns admission, provenance, facility rules and artifact
integrity; the agent owns scientific decisions; the evaluation harness measures
learning and decision quality.

## Separate review judgments

| Layer | Execution and source review | Predictive validity | Agent interpretation |
|---|---|---|---|
| 1: accuracy/attribution | Documentation only; original 27 and later 54 cohorts distinguished, 43 single-run pool labels; numerical outputs unchanged | No new validation; original 22.209 mV versus pool 48.410 mV | No canary required for documentation-only change |
| 2: facilities | Nine FastCat arms valid on actual revised wheel; two operations at one virtual process location | Not exercised | Qualified: invented 95%-style interval and single-lead winner-confirmation claim |
| 3: commands | Nine arms unchanged; installed SDL1 command proof passes and unknown-response proof fails; cleaning retains sample history | Not exercised | Qualified: one candidate's measurement still treated as winner confirmation |
| 4: protocol | Nine arms unchanged; installed 13-stage profile hash and typed missing-response/reference boundaries checked | Not exercised | Qualified: conditional winner claim and unsupported statistical-indistinguishability language |
| 5: examples | Correct-facility examples compose/run/replay; wrong facility HOLD; nine canary arms unchanged | Not exercised | Qualified: earlier 95%/global-absence wording absent, but supplied calibration details claimed absent and unmeasured rival point estimate misused as a falsifier |
| 6: validation/traceability | Both runs: nine valid arms, unchanged predictions and installed source/protocol checks; final wheel bound below | Not exercised | Qualified: first run fused FastCat/SDL1 in its physical proposal; final run omitted the recorded coverage target, misstated a rival gap and misused unmeasured rival estimates |

These judgments are scoped to observed artifacts. SDL1 refusal checks are operator
smokes, not evidence that the agent responded correctly to unsupported SDL1
requests. The FastCat prompt does not exercise every source-protocol convention.
No canary establishes improved learning, stopping, physical calibration or a
confirmed physical winner. The historical taxonomy is not a causal indictment of
the CLI or agents: wheel, examples, admission, effectively free queries,
self-authored deterministic providers and scoring incentives affected behavior.

## Review corrections

The external review found a public-path blocker: previous canaries mounted the
example README, while the public prompt alone did not teach facility selection.
Compose now resolves the installed bundle from the requirement's declared source
workstation. An explicit selector remains an override, with runnable HOLD recovery.
The implicit and explicit FastCat compositions are byte-identical in a direct test.

The skill's stale transfer and applied-value rules are corrected; its three added
reporting sentences are removed. The prior canaries did not establish that those
sentences improved behavior. Public prompt and candidate bytes remain unchanged.

SDL1 admission now uses the explicit example recipe rather than a cross-product
of unrelated defaults and cleaning commands. Arduino owns temperature setup;
Squidstat device channels no longer assert deposition-only hardware limits.
OER setup context is pinned and campaign timing includes only the known 550 s CP
dwell lower bound. The obsolete estimator and its bundled calibration records
were subsequently removed; its failed result remains in Git history.

The campaign schema already uses facility-derived action/channel vocabularies;
changing a facility does not change that format. No schema version bump or
restoration of nonexistent operations is needed. All intentional contract changes
are listed in the single staged changelog entry. Frozen FastCat predictions and
interval width remain unchanged. Source bindings and composition identities change
where the corrected declarations change.

Traceability now separates substantive evidence rows from compact declarations
of software choices and unknown physical properties. These declarations do not
cite themselves or schema slots as physical evidence. Tests check integrity and
coverage, not factual truth. The public canary index retains IDs and digests;
private paths remain only in the operator's records.

## Closed canary records

The harness uses an isolated CLI-only world, read-only inputs, a per-session
installation of the exact revised wheel and fresh isolation proof. It does not
mount outcome corpora or use port 8471. Each completed run checks 26 isolation
invariants before and after. The relay reserves budget before forwarding and
settles only authenticated complete upstream usage records; uncertain requests
retain their reservation. The authorized total cap is $10.

Recorded receipts are operator-held local artifacts, not included physical data.
`reference-lab-canaries.json` binds the audited receipts and tested wheels. Public
readers can inspect this summary and code, but should not infer access to private
receipts or independent reproduction. Two early harness incidents are retained:
PR2's first isolation check stopped before provider calls; PR3 had a transport
failure and native continuation after relay error handling was corrected. They
are execution incidents, not scientific results.

Before this external review, the wheel tested at `3ee0de743d2a2762caef7bda890745154c056f34`, has SHA256
`25addf4321f9daab24ddae3ca85207fa401c314c9a54e2a44fa545b0a54fbd14`.
That earlier canary passed nine arms with unchanged predictions, 26 pre/post
isolation checks, 89 SDL1 and 27 FastCat current installed-source hash checks,
and the pinned 13-stage protocol smoke. An operator-only negative composition
also verified that FastCat rejects SDL1's protocol selector with typed missing/
unknown-parameter HOLD reasons. The agent itself did not exercise that refusal.

The first PR6 canary used the preceding wheel and remains in the record. A
citation-only correction required the second run to bind the exact final wheel;
it was not a retry selected for a better report. The first proposal mixed
FastCat synthesis with SDL1 measurement and miscounted nine candidates' pairs
as 45 instead of 36. The second proposal did not repeat that platform fusion, but
omitted the declared 0.9 coverage target and reported a nearest-rival gap of
27.7 mV rather than 18.643 mV. Both still treated comparisons with unmeasured rival
point estimates as predictive contradictions. A coverage target is not a
confidence guarantee, and a consistent lead measurement cannot confirm ranking.
These are retained interpretation failures, not silently corrected agent outputs.

Total accounted budget was $3.249088970 of $10, including retained reservations
for calls without verified settlement. The owned relay was stopped after the
final run. The failed calibration gates remain unchanged. The full-facility
release restriction was subsequently superseded by the scope above.
Subsequent validation-summary edits are outside the wheel;
its final bytes are rechecked after those edits.

## Public-prompt-only review canary

Run `RUN-20260907T151647Z-66a44224` completed with exit 0 on wheel SHA256
`9312be9a00bf77fd927054c04e586d8e89f98e7b0ebbeb65896103ef3f4fe45b`.
Only the exact public README prompt block, candidate-set YAML, requirement YAML
and corrected skill were supplied. The example README was absent; the setup
script only placed the input directory, without CLI runs or routing hints.

All nine arms used FastCat only, retained the supplied fractions and fixed
conditions, validated and reproduced the frozen predictions and constant width.
Both 26-check isolation proofs passed. The agent initially queried the default
measurement capability and corrected its selector; it completed without operator
intervention. This clears the public-path stall for this run, not every possible
request or an SDL1 physical workflow.

Interpretation remains qualified. The report correctly identifies a virtual lead
and no current confirmed winner, but calls interval overlap statistical
indistinguishability and says a future lead measurement could confirm a winner
against unmeasured rivals. It does not report the coverage target or the two
calibration cohorts. These findings remain in the raw report; no improvement in
scientific learning or stopping is inferred from this run or the skill reversal.

Final local checks: 326 passed, eight optional Isaac/USD skips; lint, formatting,
lock, pinned plugin checks and inspection of 138 wheel/sdist members passed.
Each updated intermediate PR head also passed CI. The prompt-only run incurred
$0.007537555 in authenticated provider charges over 18 requests. Total accounted
budget is $3.256626525 of $10, including earlier unsettled reservations. The owned
8991 relay was stopped. No calibration gate, merge or release was performed.

## Approved workflow release scope

The subsequent documentation revision adopts SDL1 as a source-verified virtual
workflow and FastCat as a bounded empirical predictor. It removes physical
bench calibration as a release prerequisite and removes the obsolete estimator.
The public prompt, candidate inputs and skills are unchanged.

Rebuilt wheel SHA256:
`af5ff0900d0e3c1dc2128cd953fd622379c8d63a193afaf0c6841c0bc71da6c0`.
Compared with the prompt-only canary wheel above, only `bundle/TRACEABILITY.md`,
README-derived package metadata and the wheel RECORD changed. Runtime, contracts,
models and skills are byte-identical. No new canary was run for this documentation
change; earlier canary conclusions retain their stated limits.

Verification after the scope revision: 326 tests passed, eight optional Isaac/USD
checks skipped; build and documentation diff checks passed.
