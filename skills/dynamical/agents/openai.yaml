interface:
  display_name: "Dynamical"
  short_description: "Investigate materials decisions with validated evidence"
  default_prompt: "Use $dynamical to investigate this materials decision. Confirm the objective, evidence requirements, search space, budget, comparison rule, and stopping rule. Run and validate bounded virtual experiments, then return the supported decision, uncertainty, rival hypotheses, all experiment outcomes, and the next physical experiment."
