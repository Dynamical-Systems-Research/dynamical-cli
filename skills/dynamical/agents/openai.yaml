interface:
  display_name: "Dynamical"
  short_description: "Compose and run evidence-bound virtual laboratories"
  default_prompt: "Use $dynamical to investigate this materials objective through admitted virtual capabilities, adapt the campaign as validated evidence changes, and return the evidence behind the next physical request or HOLD."
